apiVersion: v1
kind: Secret
metadata:
  name: alexander-secrets
  labels:
    app: alexander-storage
type: Opaque
stringData:
  # Generate with: openssl rand -hex 32
  # IMPORTANT: Replace these with secure random values before deploying!
  ALEXANDER_AUTH_MASTER_KEY: "REPLACE_WITH_SECURE_64_CHAR_HEX_VALUE_FOR_ACCESS_KEY_ENCRYPTION"
  ALEXANDER_AUTH_SSE_MASTER_KEY: "REPLACE_WITH_SECURE_64_CHAR_HEX_VALUE_FOR_SSE_ENCRYPTION_KEY"

---
# Example of using external secret management (External Secrets Operator)
# Uncomment and configure based on your secret store
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: alexander-secrets
#   labels:
#     app: alexander-storage
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: alexander-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: ALEXANDER_AUTH_MASTER_KEY
#       remoteRef:
#         key: secret/data/alexander
#         property: master_key
#     - secretKey: ALEXANDER_AUTH_SSE_MASTER_KEY
#       remoteRef:
#         key: secret/data/alexander
#         property: sse_master_key
